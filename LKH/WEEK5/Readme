## BOJ 벽 부수고 이동하기

처음 생각했던 로직:
1. 백트래킹으로 배양액의 위치 고른다.
2. 각각의 배양액의 위치에 따라 bfs를 돌린다. 이때 1초를 레벨별로 처리한다.
3. 세트에 배양액이 퍼질 수 있는 좌표를 넣어줘서 중복 처리한다.
4. 세트를 리스트로 바꿔서 정렬한 후 순차 탐색해서 같은 좌표가 있다면 방문처리하고 
   꽃의 개수를 한 개 늘린 뒤 리스트에서 제거한다.
5. 꽃의 개수를 최대값으로 갱신한다.

하지만 bfs의 과정속에서 불필요한 부분이 많았고 이로인해 시간초과가 발생.

개선한 로직:
1. 백트래킹으로 배양액의 위치 고른다.
2. 방문처리를 할 배열을 3차원으로 선언
  # visited[x][y][z] : x, y는 좌표 z는 [a, b]의 형태로서
  # a는 상태를 의미함. 즉 0,1,2,3 각각 아무것도 없음, 초록색, 빨간색, 꽃을 의미함
  # b는 시간을 의미함
3. bfs를 돌린다.
4. 꽃의 개수를 최대값으로 갱신한다.