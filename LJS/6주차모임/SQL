```sql
SELECT U.USER_ID, U.NICKNAME, sum(B.PRICE) AS TOTAL_SALES
from USED_GOODS_BOARD B left join USED_GOODS_USER U ON U.USER_ID = B.WRITER_ID
where B.STATUS = 'DONE'
group by U.USER_ID
having TOTAL_SALES >=700000
order by TOTAL_SALES;
```
